"""
URLConf for the 'site_settings' app.
Placeholder for Phase 1 — real views/urls land when this app's phase
is built (see project PHASES doc). Kept as a real, importable module
now so the root urls.py include() chain is stable across all phases.
"""
from django.urls import path

app_name = 'site_settings'

urlpatterns = [
    # Populated in the phase dedicated to this app.
]
